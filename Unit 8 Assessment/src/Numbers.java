import java.util.List;

public class Numbers 
{
	private static int numba = 0;
	private static int[] numArray = {};
	
	public static boolean isGoofy(int num)
	{
		int sum = 0;
		for(int i = 0; i > numArray.length; i++)
		{
			if (num % numba == 0)
			{
				void add(numArray.length,numba);
			}
			sum += numArray[i];
		}
		if (sum % 2 == 0)
		{
			return false;
		}
		else
		{
			return true;
		}
		
	}
	public static int[] getSomeGoofyNumbers(int num)
	{
		
	}
}
